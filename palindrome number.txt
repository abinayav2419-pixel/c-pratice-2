# include <stdio.h>
int main(){
    int n,temp,digit,rev = 0;
    
    scanf("%d",&n);
    
    temp = n;
    
    for(;n!=0;n/=10){
        digit = n%10;
        rev =rev*10+ digit;
    }
   if(rev==temp)
   printf("yes");
   else
   printf("no");
   
   return 0;
}