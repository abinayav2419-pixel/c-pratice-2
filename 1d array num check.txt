# include <stdio.h>
int main(){
    int n;
    printf("Enter numers of elemets:");
    scanf("%d",&n);
    
    int arr[n];
    printf("Enter the elemets:");
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    
    int increasing=1;
    
    for(int i=0;i<n-1;i++){
    if(arr[n]>=arr[n+1]){
        increasing=0;
        break;
    }
    }
    
    if(increasing){
        printf("yes",increasing);
    }else{
        printf("No",increasing);
    }
   return 0; 
}